import 'package:flutter/material.dart';

class HemisId extends StatelessWidget {
  const HemisId({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
